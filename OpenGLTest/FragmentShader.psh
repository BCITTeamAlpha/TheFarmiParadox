#version 330 core

in vec4 v_color;
in vec3 v_position;
in vec3 v_normal;

out vec4 o_fragColor;

uniform vec3 lightPosition;

void main() {
	vec3 N = normalize(v_normal);
	vec3 L = lightPosition - v_position;

	o_fragColor = v_color * dot(N, normalize(L)) / length(L);
}